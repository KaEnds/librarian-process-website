version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
    # Uncomment if you have environment variables to pass
    # env_file:
    #   - .env.local
